import { Text } from "react-native";

function CalfRearing({ navigation }) {
  return (
    <Text onPress={() => navigation.navigate("CalfRearing")}>CalfRearing</Text>
  );
}

export default CalfRearing;
